<?xml version="1.0" encoding="UTF-8"?>
<head>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
</head>
<mapper namespace="#class.package#.model.#class.name#">
	<resultMap id="BaseResultMap" type="#class.package#.model.#class.name#">
		#columns.mapping#
	</resultMap>

	<sql id="tb">
	    #table.name#
	</sql>

	<sql id="cols_all">
	    #id#,<include refid="cols_exclude_id"/>
	</sql>

	<sql id="cols_exclude_id">
	  #cols#
	</sql>

	<sql id="vals">
	   #vals#
	</sql>
</mapper>

	<resultEntry><#Entry# property="#EntryValue#" column="#EntryKey#"/></resultEntry>
	
	<ifEntry>	
		<if test= #EntryValue# != null">
			#joiner# EntryKey = #{EntryValue}
		</if>
	</ifEntry>
	
	<valueEntry>
		#joiner# #{#value#}
	</valueEntry>